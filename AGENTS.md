# WHP Standing canonical source

Work from the finished recursive object: A acquires Mark A; B independently verifies it, discovers WHP Standing, and may acquire Mark B for B’s own eligible object.

Preserve fail-closed authority, exact COPY and identity-preserving COMPOSE evaluation, standard exact EIP-3009 payment, durable atomic payment binding, independent append-only rollback witnessing, byte-identical retrieval, recovery without recharge, independent verification, and strict TEST/LIVE separation.

Payment never creates authority or guarantees a Mark. Recursive discovery never delegates issuance. No buyer-specific proof, login, registration, or proprietary client is required for the public acquisition path.

The repository must remain deployment-agnostic. All public requests pass through `src/app.mjs`. Infrastructure adapters may translate transport only and may not redefine routes or protocol semantics. Do not add deployment-service configuration, implicit origin aliases, managed-database imports, embedded credentials, private keys, or origin-bound authority material.

Unknowns remain unknown. Do not label implementation evidence as deployment, LIVE operation, sale, issuance, outside demand, or propagation proof.
